# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นาย ธดากรณ์ เชื้อโต่ง **รหัส:** 68543210052-5 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 14:00 | B1 | การใช้ prop ที่ถูกต้อง | อธิบายหลักการ One-Way Data Flow, การส่ง/รับ Props ด้วย Destructuring และกฎ Read-Only ห้ามแก้ไขค่าตรงๆ | นำหลักการ Destructuring ไปใช้ออกแบบการรับส่ง Props ระหว่าง DashboardPage และ Component ลูก | 
| 14:17 | B1 | ต้องการใช้งานตัวกรองข้อมูลมาแสดงในแต่ละ button ที่กรองข้อมูลสถานะมีตัวอย่างไหม | แนะนำโครงสร้าง FilterBar, การใช้ State จัดการสถานะที่เลือก และการใช้ Derived State (.filter()) คำนวณข้อมูลแสดงผล | สร้างคอมโพเนนต์ FilterBar.jsx และเขียนฟังก์ชันจัดการเงื่อนไขการกรองข้อมูลในหน้า Dashboard |
| 14:38 | B1 | const filters ถ้าอยู่นอกฟังชั่นได้ไหม / ถ้าชุดข้อมูลเป็นแบบ 2D Array ละ | อธิบาย Best Practice เรื่องการวาง Static Constant ไว้นอกฟังก์ชันเพื่อลดภาระ Re-render และวิธี Destructure อาร์เรย์ [key, label] ใน .map() | ย้ายตัวแปร filters ไว้นอกฟังก์ชัน Component และปรับโค้ด .map([filterValue, label]) เพื่อสร้างปุ่ม |
| 14:50 | B1 | คิดว่าของเดิมมันถูกน๊าแต่ไหงตอนกดแต่ละอันข้อมูลขึ้นไม่ตรง / ตอนกดปุ่มต่างๆ สถานะมันขึ้นแค่ pending | แนะนำจุดตรวจเช็ค: การดักเงื่อนไขค่า 'all', การตรวจสอบชื่อ Props ฟังก์ชัน Callback และการป้องกันการเขียน State หลักทับ | ตรวจสอบฟังก์ชัน .filter() ใน DashboardPage ให้คืนค่าข้อมูลทั้งหมดเมื่อเลือก 'all' และเช็คชื่อ Props ที่ส่งไปยัง FilterBar |
| 15:15 | B1| ตัวเลข "เสร็จสิ้น" ในแผงสรุปไม่ตรงกับที่เห็นจริง / แก้ให้ตรงหมดแล้ว เป็น 0 หมดเลย | ชี้แจงเรื่องการนับจาก State ข้อมูลเต็ม (requests) ไม่ใช่นับจากตัวที่กรองแล้ว, การตรวจชื่อ Key ใน Object และการใส่ ?? 0 ป้องกันค่าว่าง | เพิ่ม Fallback Value ?? 0 ใน SummaryPanel.jsx และตรวจสอบชื่อคีย์ (total, pending, inProgress, completed) ให้ตรงกัน |
| 15:40 | B1 | ทำไม useMemo ได้ข้อมูลออกมาเป็น 0 ตลอดเวลา | ตรวจพบว่า useMemo สำหรับคำนวณ summary มีการใส่ Dependency เป็น Array ว่าง [] ทำให้จำค่าเริ่มต้นที่ยังไม่มีข้อมูล (Stale Value) | แก้ไข Dependency Array ของ useMemo จาก [] เป็น [requests] เพื่อให้ระบบคำนวณตัวเลขสรุปใหม่เมื่อโหลดข้อมูลเสร็จ |
| 15:59 | B1 | ขอตัวอย่างการใช้ Route Params เปลี่ยนตาม url | อธิบายเรื่อง Component Reuse ใน React Router เมื่อเปลี่ยน Param ใน URL ต้องระบุ requestId ลงใน Dependency Array ของ useEffect เพื่อสั่งโหลดใหม่ | ใส่ [requestId] ต่อท้าย useEffect ใน RequestDetailPage.jsx เพื่อให้ดึงข้อมูลใหม่ทุกครั้งที่ URL เปลี่ยน |
| 16:07 | B1 | พอกดปุ่มลบที่การ์ดแล้วหน้าเว็บแครชเป็นจอขาวทันที ใน Console ขึ้น TypeError: requests.filter is not a function เกิดจากอะไร | อธิบายว่า deleteRequest เป็นฟังก์ชัน Async ที่คืนค่าเป็น Promise หากไม่ใส่ await ตัวแปรที่นำไปอัปเดต State จะเป็น Promise Object ไม่ใช่ Array ส่งผลให้ขั้นตอน Re-render ที่เรียกใช้ .filter() หรือ .map() แครชจนหน้าขาว | ใส่ await หน้าคำสั่ง deleteRequest(requestId) ในฟังก์ชัน handleDelete ของไฟล์ DashboardPage.jsx แล้วทดสอบกดลบใหม่ |
| 16:15 | B1 | พอลบคำร้องแล้วการ์ดหายไปจากหน้าจอได้ปกติ แต่ตัวเลขจำนวนใน SummaryPanel ด้านบนไม่ยอมลด ค้างอยู่ที่เลขเดิมตลอด | อธิบายเรื่องกลไก useMemo คำนวณก้อน summary หากใส่ Dependency Array เป็นค่าว่าง [] React จะจดจำค่าเริ่มต้นไว้ตลอดและไม่คำนวณใหม่เมื่อข้อมูลเปลี่ยน ต้องระบุ [requests] ลงใน Dependency | ตรวจสอบบรรทัดคำนวณ summary ใน DashboardPage.jsx และแก้ไข Dependency Array ของ useMemo จาก [] เป็น [requests] |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |


*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ______________________
